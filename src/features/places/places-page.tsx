import { RoutePlaceholder } from '@/app/route-placeholder'

/** B-02 — Gestión de lugares (Place). Sin implementar (ver plan). */
export function PlacesPage() {
  return <RoutePlaceholder label="lugares — pendiente" />
}
