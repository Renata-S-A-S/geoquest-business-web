import { RoutePlaceholder } from '@/app/route-placeholder'

/** B-03 — Crear/gestionar recompensas (Reward). Sin implementar (ver plan). */
export function RewardsPage() {
  return <RoutePlaceholder label="recompensas — pendiente" />
}
