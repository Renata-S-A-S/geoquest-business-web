import { RoutePlaceholder } from '@/app/route-placeholder'

/** B-05 — Dashboard de analytics del negocio. Sin implementar (ver plan). */
export function AnalyticsPage() {
  return <RoutePlaceholder label="analytics — pendiente" />
}
