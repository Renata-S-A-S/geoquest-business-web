import { RoutePlaceholder } from '@/app/route-placeholder'

/** B-01 — Registro y verificación del negocio. Sin implementar (ver plan). */
export function OnboardingPage() {
  return <RoutePlaceholder label="negocio — pendiente" />
}
