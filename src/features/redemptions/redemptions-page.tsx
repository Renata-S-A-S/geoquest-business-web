import { RoutePlaceholder } from '@/app/route-placeholder'

/** B-04 — Validar canje por QR. Sin implementar (ver plan). */
export function RedemptionsPage() {
  return <RoutePlaceholder label="validar canje — pendiente" />
}
